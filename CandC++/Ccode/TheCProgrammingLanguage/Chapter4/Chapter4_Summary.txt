
=========================Chapter4分割线=========================


p33 首次提到标准头文件 <string.h> ，以及其中的strlen函数。


p34 首次出现关于const的内容。


p56 首次提到标准库中的strstr函数。


p58 首次提到<stdlib.h>库中的atof函数。

p59 “空参数表的这种特殊意义是为了使较老的 C 程序可以被新的编译器编译。但是，在新程序中也这样做是不明智的。如果函数有参数，请声明它们；如果没有参数，请使用 void。”

p59 “因此，当函数 atof 出现在上述程序的return 语句中时，其返回的 double 类型的值将被自动转换为 int 类型。但是，这一操作有丢失信息的可能，因此一些编译器会对此给出警告。而此函数中的强制转换显式地表明了所要进行的操作，屏蔽掉了相关的警告信息。”

p65 “弄清楚外部变量的声明和定义二者间的区别很重要。声明只是标明了变量的属性（主要是
它的类型）；而定义还会为其留出存储空间。”


=========================Chapter4分割线=========================

page56-57 开头部分声明getline子函数时末尾少了个分号。

page58-59 程序中出现了“%g”格式控制。也就是按double类型打印的格式控制符之一，详情参见附录B.1.2，也就是209页的内容。

page61-64 那个逆波兰计算器的大程序也有两处错。详情参见具体文件。