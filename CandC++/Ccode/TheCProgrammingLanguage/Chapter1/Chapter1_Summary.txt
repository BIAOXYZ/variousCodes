ctrl+z是结束，也就是那个EOF。

=========================Chapter1分割线=========================

p15-16 “有经验的 C 程序员可以把这个拷贝程序写得更精练一些。 C 语言中任何赋值语句，比如c = getchar(); 都是一个表达式，并且有自身的值。它的值就是赋值之后等号左边变量的值。这就意味着，赋值语句可以作为更大的表达式的一部分出现。”


=========================Chapter1分割线=========================


page16 那个程序讲到了"语句c = getchar() != EOF  等价于 c = (getchar() != EOF)"，所以我们一般那一对外层的括号是不能省略的，即常用形式为"(c = getchar()) != EOF"。
page20 怎样往命令行里复制内容。
page27-28 中文版书籍源代码有个小错误，已在程序注释中说明。另外首次引入extern类型的变量（外部变量）。还有，对于auto类型的变量（自动变量）那个“自动”的解释挺好。